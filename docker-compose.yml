services:
  synology-auto-bot:
    build: .
    container_name: synology-auto-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${SYNOLOGY_TORRENT_WATCH_DIR_HOST:-/usbshare2/torrentDown/torrent_watch}:${SYNOLOGY_TORRENT_WATCH_DIR:-/watch}
